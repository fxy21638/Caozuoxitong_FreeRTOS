#ifndef __GUI_H
#define __GUI_H

#include "stm32f4xx.h"
#include <stdint.h>
#include "FreeRTOS.h"
#include "queue.h"

/* ========================================================================== */
/*  gui.h — 触摸屏 GUI 类型定义与接口                                           */
/* ========================================================================== */

/* 历史记录深度 */
#define GUI_HISTORY_DEPTH   12

/* ========================================================================== */
/*  传感器类型 (GUI 内部使用)                                                     */
/* ========================================================================== */
typedef enum {
    GUI_SENSOR_DHT     = 1,
    GUI_SENSOR_MPU6050 = 2,
} GUISensorType_t;

/* ========================================================================== */
/*  GUI 消息结构 (其他任务 → GUI 任务传递传感器数据)                               */
/* ========================================================================== */
typedef struct {
    GUISensorType_t type;
    union {
        struct { float temp; float humi; } dht;
        struct { float pitch; float roll; float yaw; } mpu;
    } data;
} GUIMsg_t;

/* ========================================================================== */
/*  公开接口                                                                     */
/* ========================================================================== */
void GUI_Task(void *pvParameters);

#endif /* __GUI_H */
